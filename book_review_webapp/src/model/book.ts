export type BookResponse = {
    isbn: string
    title: string
    description: string
    image_url: string
    author: string
    release_date: Date
}

export type BookRequest = {
    isbn: string
    title: string
    description: string
    image_url: string
    author: string
    release_date: Date
}